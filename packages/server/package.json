{
  "name": "@acme/server",
  "main": "./index.d.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run server.ts",
    "clean": "bunx rimraf dist",
    "build": "bun run clean && bunx tsc"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2"
  }
}
